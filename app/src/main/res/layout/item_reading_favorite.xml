<?xml version="1.0" encoding="utf-8"?>
<!--
    每日一读收藏夹列表项：替代原先的 android.R.layout.simple_list_item_1
    （Java 里靠 setTextColor/setPadding 手工补样式）。
    ListView 的 item 不支持 root margin，故用外层透明容器 padding 制造卡片间距。
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:paddingStart="8dp"
    android:paddingEnd="8dp"
    android:paddingTop="3dp"
    android:paddingBottom="3dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:background="@drawable/bg_session_item"
        android:paddingStart="12dp"
        android:paddingEnd="12dp"
        android:paddingTop="10dp"
        android:paddingBottom="10dp">

        <ImageView
            android:layout_width="34dp"
            android:layout_height="34dp"
            android:background="@drawable/bg_reading_favorite_icon"
            android:padding="8dp"
            android:src="@drawable/baseline_book_24"
            android:tint="@color/theme_primary"
            android:scaleType="fitCenter"
            android:contentDescription="@null" />

        <TextView
            android:id="@+id/tv_favorite_title"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:layout_marginStart="10dp"
            android:maxLines="2"
            android:ellipsize="end"
            android:lineSpacingExtra="2dp"
            android:textSize="14sp"
            android:textStyle="bold"
            android:textColor="@color/reader_text" />
    </LinearLayout>
</LinearLayout>
